<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Premium B — Dark Theme overrides -->
    <style name="Base.Theme.Mahout" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/mahout_bg</item>

        <item name="android:statusBarColor">@color/mahout_bg</item>
        <item name="android:navigationBarColor">@color/mahout_bg</item>

        <!-- Dark bars = light icons -->
        <item name="android:windowLightStatusBar">false</item>
        <!-- DO NOT set android:windowLightNavigationBar (API 27+) -->
    </style>

</resources>
